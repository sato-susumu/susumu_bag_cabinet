# テストガイド

## 自動テスト

### 基本コンポーネントテスト
```bash
python test_basic.py
```

このテストでは以下を確認します:
- モジュールのインポート
- 設定管理
- bagファイルのスキャン
- ファイル名生成
- サイズフォーマット
- bagファイル情報の取得

### 閲覧機能テスト
```bash
python test_browse.py
```

このテストでは実際のbagファイルを使って:
- ファイルスキャン
- 詳細情報の取得
- フォーマット検出
- サイズ計算

### GUIテスト
```bash
python test_gui.py
```

このテストではGUIの:
- ウィンドウ起動
- ページ切り替え
- 基本的なナビゲーション

## 手動テスト

### アプリケーションの起動

```bash
# 方法1: 起動スクリプトを使用
./run.sh

# 方法2: Pythonモジュールとして起動
python -m susumu_bag_cabinet.main

# 方法3: インストール後のコマンド
susumu-bag-cabinet
```

### テストシナリオ

#### 1. ホーム画面
- [x] アプリケーションが起動する
- [x] 3つの大きなボタンが表示される
- [x] ボタンのラベルが正しい（記録する、記録をみる、設定をひらく）

#### 2. 設定画面
- [x] 「設定をひらく」をクリック
- [x] 設定画面が表示される
- [x] 保存先フォルダが表示される
- [x] 「参照」ボタンでフォルダを選択できる
- [x] ロボット名を入力できる
- [x] ファイル名プレビューが更新される
- [x] Foxgloveコマンドを変更できる
- [x] 「保存してホームへ」でホームに戻る

#### 3. 記録画面
- [x] 「記録する」をクリック
- [x] 記録画面が表示される
- [x] 保存先フォルダが表示される
- [x] ファイル名プレビューが表示される
- [x] ラベルを入力するとファイル名プレビューが更新される
- [ ] 「記録開始」をクリックして記録が開始される
  - 注: ROS2のトピックが存在する環境で実行してください
- [ ] 経過時間が更新される
- [ ] ファイルサイズが更新される
- [ ] 「記録停止」で記録が停止される
- [x] 記録中は「ホームへ戻る」が無効化される

#### 4. 閲覧画面
- [x] 「記録をみる」をクリック
- [x] 閲覧画面が表示される
- [x] フォルダパスが表示される
- [x] bagファイル一覧が表示される
- [x] ファイル情報が表示される（ファイル名、サイズ等）
- [x] プログレスバーが表示される
- [x] バックグラウンドでメタデータが取得される
- [x] チェックボックスで選択できる
- [x] 「全選択」「選択解除」ボタンが動作する
- [ ] Foxglove Studio がインストールされている場合:
  - ファイルを1つ選択
  - 「Foxgloveで再生」をクリック
  - Foxglove Studioが起動してファイルが開かれる

## 既知の制限事項

1. **記録機能**: ROS2環境が必要です。トピックが存在しない環境では記録できません。
2. **圧縮機能**: 未実装です。
3. **修復機能**: 未実装です。
4. **Foxglove再生**: Foxglove Studioがインストールされている必要があります。
5. **bagファイル検出**: metadata.yamlが存在するbagファイルのみを検出します。

## トラブルシューティング

### アプリケーションが起動しない
```bash
# PySide6がインストールされているか確認
pip show PySide6

# 再インストール
pip install -e .
```

### bagファイルが検出されない
- metadata.yamlが存在するか確認
```bash
ls ~/ros2_bags/*/metadata.yaml
```

### ros2 bag info が動作しない
- ROS2環境がセットアップされているか確認
```bash
source /opt/ros/humble/setup.bash  # または iron, jazzy など
ros2 bag --help
```

### Foxglove Studioが起動しない
- コマンドが正しいか確認
```bash
which foxglove-studio
```

## パフォーマンステスト

大量のbagファイル（100個以上）がある環境でテストする場合:
1. スキャンに時間がかかることを確認
2. バックグラウンドでメタデータが順次取得されることを確認
3. UIがフリーズしないことを確認

## セキュリティテスト

- ファイルパスに特殊文字が含まれる場合の動作
- 読み取り権限がないファイルの処理
- 破損したbagファイルの処理
